user root;
worker_processes auto;
pid /run/nginx.pid;
include /etc/nginx/modules-enabled/*.conf;

events {
    worker_connections 768;
}

http {
    client_max_body_size 20M;
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;

    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    ssl_protocols TLSv1 TLSv1.1 TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;

    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    gzip on;

    server {
        listen 80;
        server_name localhost;

        add_header Access-Control-Allow-Origin *;
        add_header Access-Control-Allow-Headers X-Requested-With;
        add_header Access-Control-Allow-Methods GET,POST,OPTIONS;

        # 推理流式接口 - 直连宿主机 Python 推理服务（绕过 backend 容量限制）
        # 前端请求 /api/v1/* 会被转发到 http://host.docker.internal:9060/v1/*
        location ^~ /api/v1/ {
            proxy_pass http://host.docker.internal:9060/v1/;
            proxy_set_header Host $host;
            proxy_set_header Connection "";
            chunked_transfer_encoding off;
            proxy_set_header X-Accel-Buffering off;
            add_header X-Accel-Buffering off;
            proxy_http_version 1.1;
            proxy_buffering off;
            proxy_cache off;
            sendfile off;
            tcp_nodelay on;
            proxy_read_timeout 600s;
            proxy_send_timeout 600s;
        }

        # 其它后端 API - 走 backend 服务（房间/服务列表/登录等）
        location /api {
            proxy_pass http://backend:8021;
            proxy_set_header Host $host;
            proxy_set_header Connection "";
            chunked_transfer_encoding off;
            proxy_set_header X-Accel-Buffering off;
            add_header X-Accel-Buffering off;
            proxy_http_version 1.1;
            proxy_buffering off;
            proxy_cache off;
            sendfile off;
            tcp_nodelay on;
        }

        # WebSocket - LiveKit
        location /ws {
            proxy_pass http://livekit:7880;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_cache_bypass $http_upgrade;
        }

        # RTC - LiveKit
        location /rtc {
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "Upgrade";
            proxy_set_header Host $host;
            proxy_cache_bypass $http_upgrade;
            proxy_pass http://livekit:7880;
        }

        # 前端静态文件
        location / {
            root /usr/share/nginx/html;
            index index.html index.htm;
            try_files $uri $uri/ /index.html;
        }

        location @router {
            rewrite ^.*$ /index.html last;
        }
    }
}
